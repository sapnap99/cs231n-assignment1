# PyCharm 本地运行

1. 在 PyCharm 中打开此 assignment1 文件夹，选择你已安装依赖的 Python 解释器（Python 3.12 或更新版本）。
2. 在 PyCharm 终端运行 `python -m pip install -r requirements-local.txt`。已安装的依赖会跳过；six 和 future 是原作业额外需要的兼容库。
3. 运行 `python local_setup.py` 下载并校验 CIFAR-10（约 163 MB）。Notebook 第一格也会自动完成这一步。
4. 在 PyCharm 中运行 `check_environment.py`，检查当前解释器依赖，并实际读取 CIFAR-10 测试批次。若缺少库，脚本会列出安装命令。
5. 按 knn、softmax、two_layer_net、features、FullyConnectedNets 的顺序打开 Notebook。选择同一个解释器，从第一格开始逐格运行。
6. 若 PyCharm 无法直接执行 Notebook，在此目录的终端运行 `python -m notebook`，使用浏览器打开。

五个作业 Notebook 的第一格已适配本地环境。算法 TODO 和文字题仍需完成，尚不能直接运行整份作业得到最终结果。原始 Colab 文件保留在上级 assignment1_colab.zip。

collect_submission.ipynb 仍用于 Colab：完成作业后将整个文件夹（不含虚拟环境）上传到 Drive，修改其中 FOLDERNAME，再运行以生成 ZIP 和 PDF。
