# Assignment 1 提交文件

最终文件位于 `output/pdf/`：

- `a1_code_submission.zip`：五个已运行 Notebook、cs231n 源代码、已训练模型及本地数据准备脚本。
- `a1_inline_submission.pdf`：按 Q1 到 Q5 顺序合并，共 57 页，含代码、真实运行输出、图表和 12 道文字答案。

压缩包不含 CIFAR-10 数据集、虚拟环境和页面检查用图片。无需重新训练即可提交这两个文件。实际提交平台和截止日期以任课老师通知为准；尚未上传到任何平台。

## 修改后重新打包

在 assignment1 目录使用现有 Python 环境执行：

```powershell
python -m pip install -r requirements-submission.txt
python -m playwright install chromium
python build_submission.py
```

该脚本仅导出 Notebook 已保存的输出，不重新运行训练。数学公式渲染资源需要网络连接。若修改了算法或实验代码，先运行对应 Notebook 并保存输出，然后重新打包。`tmp/pdfs/` 是可清理的中间文件目录。
